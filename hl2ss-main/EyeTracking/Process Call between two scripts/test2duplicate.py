def process_data(shared_data):
    print("Running process_data in test2.py")
    # Perform some computation
    result = 42  # Example computation result

    # Store the result in the shared dictionary
    shared_data['result'] = result

    print("test2.py has finished executing and stored the result.")
